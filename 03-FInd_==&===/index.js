/** 1. == and === both are comparison operator.
 *  2. The main differences between both the operators is that,
 *     "==" is used to compare only value whereas ," === " is used to compare both value and types.
 */

var x = 7;
var y = "7";
console.log(x == y);
console.log(x === y);
